# Deployment Configuration
# Copy this file to .env.deploy and fill in your values
# DO NOT commit .env.deploy to the repository

# AWS SSO Profile name (from `aws configure sso`)
# Run `aws configure list-profiles` to see available profiles
AWS_PROFILE=your-aws-sso-profile-name

